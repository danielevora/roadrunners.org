
<div style="height:300px;width:300px;">
    <script type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyA1feUiRmNm-IBvRmjnuP9BuzJtEc7sJ7A&sensor=true">
    </script>
    <script type="text/javascript">
    var map;
    
      function initialize() {
        var mapOptions = {
          center: new google.maps.LatLng(33.700866, -117.777155),
          zoom: 11
        };
        map = new google.maps.Map(document.getElementById("map-canvas"), mapOptions);
        
      }
      google.maps.event.addDomListener(window, 'load', initialize);
      console.log(getLocation("7 Snapdragon Irvine, CA 92604"));
      // createMarker(function (location) {
      	// location = getLocation("7 Snapdragon Irvine, CA 92604");
			// });
      
      //getLocation(functin()"7 Snapdragon Irvine, CA 92604")
      

      
      function getLocation(address) {
        var geocoder = new google.maps.Geocoder();
				var location = "";
				geocoder.geocode( { 'address': address, 'region': 'US' }, function(results, status) {
					if (status != google.maps.GeocoderStatus.OK)
						 return null;
						
					return results[0].geometry.location;
			    });
				}
				
				function createMarker(location) {
					if (location = null)
						return;

					var marker = new google.maps.Marker({
						position: new google.maps.LatLng(location.lat(), location.lng()),
						map: map,
						title: 'Hello World!'
					});
				}
			}
    </script>
    <div id="map-canvas"/>
</div>
